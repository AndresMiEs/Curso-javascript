//Notación

// ; --> Delimita el final de una línea (A día de hoy no son necesarias pero es una buena práctica usarla).

const b = 4;

b + 4;

// . --> Se utiliza en los objetos para acceder a los atributos.
    //Ejemplo: movil.anchura

// [] --> Listas, arreglos o arrays

const ar = [1, 2, 3, 4];
console.log(ar[2]);

//() --> Se utilizan para funciones

function suma(a, b){
    //se escribe la función
}
// {} --> Llaves para objetos, funciones y estructuras de control

const movil = {
    anchura: 5,
    altura:10,
}

if(true){
    //todo lo que haya entre llaves

    const constante2 = "Hola";
}